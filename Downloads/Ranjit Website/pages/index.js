"use client";
import { GlobeVisualization } from '../ReactGlobe';

export default function Home() {
  return (
    <div style={{ width: '100%', height: '100vh', backgroundColor: '#ffffff' }}>
      <GlobeVisualization 
        globeConfig={{
          globeColor: '#ffffff',
          ambientLight: '#ffffff',
          directionalLeftLight: '#ffffff',
          directionalTopLight: '#ffffff',
          pointLight: '#ffffff',
        }} 
      />
    </div>
  );
}"use client";
import { GlobeVisualization } from '../ReactGlobe';

export default function Home() {
  return (
    <div style={{ width: '100%', height: '100vh', backgroundColor: '#ffffff' }}>
      <GlobeVisualization 
        globeConfig={{
          globeColor: '#ffffff',
          ambientLight: '#ffffff',
          directionalLeftLight: '#ffffff',
          directionalTopLight: '#ffffff',
          pointLight: '#ffffff',
        }} 
      />
    </div>
  );
}